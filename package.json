{
  "name": "mts-load-distribution-railway",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "cd app && npm install",
    "install:app": "cd app && npm install",
    "start": "cd app && npm run start:railway",
    "test": "cd app && npm test"
  }
}
