{
  "name": "mts-load-distribution-codespaces",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:app": "cd app && npm install",
    "start": "cd app && npm run start:codespaces",
    "test": "cd app && npm test"
  }
}
