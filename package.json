{
  "name": "mts-load-distribution-local-regional-tabs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "postinstall": "cd app && npm install",
    "start": "cd app && npm start",
    "test": "cd app && npm test",
    "tabs:ensure-queues": "cd app && npm run tabs:ensure-queues"
  }
}
